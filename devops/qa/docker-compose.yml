version: '3.8'

services:
  codexy-api-qa:
    container_name: codexy-api-qa
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env.qa
    ports:
      - "5200:8080"
    networks:
      - codexy-network
    labels:
      - com.codexy.environment=qa

networks:
  codexy-network:
    external: true
